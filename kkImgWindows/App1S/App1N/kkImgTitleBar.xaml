<?xml version="1.0" encoding="utf-8"?>
<UserControl
    x:Class="App1N.KkImgTitleBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:App1N"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <TitleBar
            Title="kkImg"
            IsBackButtonVisible="False"
            IsPaneToggleButtonVisible="False">

            <TitleBar.IconSource>
                <SymbolIconSource Symbol="Emoji" />
            </TitleBar.IconSource>

            <TitleBar.Content>
                <StackPanel Orientation="Horizontal">
                    <local:kkImgMenuBar></local:kkImgMenuBar>
                </StackPanel>

            </TitleBar.Content>
            <TitleBar.RightHeader>
                <Border 
                    VerticalAlignment="Center" 
                    Background="{x:Bind ViewModel.TitleBar.UnixSecondsColor, Mode=OneWay}"
                    CornerRadius="{ThemeResource ControlCornerRadius}"
                    Padding="2,0">

                    <TextBlock 
                        Text="{x:Bind ViewModel.TitleBar.UnixSeconds, Mode=OneWay}"
                        VerticalAlignment="Center" />
                </Border>

            </TitleBar.RightHeader>
        </TitleBar>
    </Grid>
</UserControl>
